# Frontend environment configuration for Vite
#
# Set the API origin only (no trailing slash, no /api suffix).
# Example for local dev backend running on port 5000:
#   http://localhost:5000
# Example for production behind a reverse proxy:
#   https://api.your-domain.com

VITE_API_BASE_URL=http://localhost:8000

# Optional feature flags for local debugging (leave unset for production):
# VITE_FORCE_SHOE_DETECT=false
# VITE_FORCE_COIN_MATCH=false
